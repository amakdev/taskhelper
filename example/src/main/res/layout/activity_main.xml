<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.amakdev.android.taskhelperexample.MainActivity">

    <TextView android:text="Seconds to run task:" />

    <EditText
        android:id="@+id/seconds_to_run"
        android:hint="Default value is 10 seconds"
        android:inputType="number" />

    <CheckBox
        android:id="@+id/chk_should_fail"
        android:layout_marginTop="8dp"
        android:text="Should fail task" />

    <Button
        android:id="@+id/btn_run"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginTop="16dp"
        android:text="Run task" />

</TableLayout>
